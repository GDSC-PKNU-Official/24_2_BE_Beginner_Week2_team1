import Activity.*;
import Membership.*;

public class Main {
    public static void main(String[] args) {
        // 인적 사항 객체 생성
        // Information info = new Information(###);

        // 회원 객체 생성
        // Membership beginner = new Beginner(###);

        // 참여 활동 객체 생성
        // Activity study = new Study(###);

        // Person 객체 생성
        // Person person = new Person(info, beginner, study);

        // Person 정보 출력
        // person.printPersonInfo();
    }
}
